This directory stores the original publication and thesis LaTeX sources together with derived summaries.
The originals are archival inputs and should not be modified in place.
Project-facing refinements live alongside them as separate `.tex` files, currently `refined_thesis_logic.tex` and `refined_publication_summary.tex`.
